#include <stdio.h>
#include <math.h>
int main()
{
    int i,num,d,sum=0;
    printf ("Enter the Decimal: ");
    scanf ("%d",&num);
    for (i=0; num>0; i++)
    {
        d=num%2;
        sum=sum+(pow(10,i)*d);
        num/=2;
    }
    printf ("The binary: %d",sum);
    return 0;
}
