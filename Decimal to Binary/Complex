#include <stdio.h>
int main()
{
    int num,d,sum=0,i;
    printf ("Enter the Decimal: ");
    scanf ("%d",&num);
    for (i=1; num>0; i*=10)
    {
        d=num%2;
        sum+=d*i;
        num/=2;
    }
    printf ("The binary: %d",sum);
    return 0;
}
