#include <stdio.h>
int main()
{
    int num,d,sum=0,i=1;
    printf ("Enter the Decimal: ");
    scanf ("%d",&num);
    while (num>0)
    {
        d=num%8;
        sum+=d*i;
        i*=10;
        num/=8;
    }
    printf ("The Octal: %d\n",sum);
    return 0;
}
